apiVersion: v1
kind: Service
metadata:
  name: payment-kafka
  namespace: inno
spec:
  selector:
    app: payment-kafka
  publishNotReadyAddresses: true
  ports:
    - name: broker
      port: 9092
      targetPort: 9092
    - name: controller
      port: 9093
      targetPort: 9093